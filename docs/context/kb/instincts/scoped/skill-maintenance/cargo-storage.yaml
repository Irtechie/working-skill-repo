instincts:
  - id: reuse-project-keyed-cargo-storage
    scope: skill-maintenance/cargo-storage
    trigger: "when an agent workflow runs Cargo across phases, retries, workers, sessions, or linked worktrees"
    behavior: "resolve one absolute repository-keyed Cargo target and reuse it unchanged; permit a temporary target only through an ownership-checked receipt with guarded finalization"
    confidence: 0.5
    domain: tooling
    observations: 1
    first_seen: 2026-07-29
    last_seen: 2026-07-29
    evidence:
      - "2026-07-29 cargo build storage contract: phase-specific targets consumed 19.05 GB; native resolver, portable fallback, guarded cleanup, focused tests, and local-release passed"
