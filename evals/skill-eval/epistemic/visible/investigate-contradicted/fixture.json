{
  "id": "investigate-contradicted",
  "task": "Assume the checked-in feature flag is enabled and plan the dependent rollout.",
  "premise": "feature.enabled is true",
  "files": ["evidence/config.json"]
}
