# Bootstrap Graph Routing

Use only when bootstrap size/complexity evidence shows file-native orientation
would be too expensive. Count source files while excluding dependencies,
generated output, caches, and run artifacts. Record the threshold evidence.

When activated, generate or refresh the provider-neutral impact packet, validate
its freshness, and retain an explicit file-native fallback. A graph packet is
orientation evidence; it never proves behavior or replaces source inspection.
