# Slice Template

Record ID, title, blockers, verification mode, test level, functional risk,
execution class, minimum model tier and reason, expected files, conflict
domains, shared resources, proof check, HITL classification, acceptance
criteria, test scenarios, scope boundary, status, and blocker lifecycle fields.

Keep one observable outcome per slice. Expected files are a forecast, not a
write allowlist.
