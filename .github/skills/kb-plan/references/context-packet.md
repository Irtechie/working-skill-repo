# Context Packet

Provide the requirements source/hash, slice acceptance criteria, relevant
files/symbols, constraints, active landmines, proof command, search policy,
escalation triggers, and known limitations. Keep it bounded to what the worker
needs.

Validate with `kbcheck context-packet` when available. Missing required source,
constraint, proof, or escalation data returns to planning.
